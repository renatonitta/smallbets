Hi <%= @user.name %>,

Here are the top topics from the past week:

<% @grouped_rooms.each do |source, rooms| %>
<%= strip_emoji_from_name(source.name) || source.name %>
<% rooms.each do |room| %>
<% feed_card = room.automated_feed_card %>
- <%= feed_card&.title || room.name %>
<%= feed_card&.summary %>
<%= room_url(room) %>
<% end %>

<% end %>
--
To opt out of these emails, click here:
<%= mailkick_unsubscribe_url(@user, "weekly_digest") %>